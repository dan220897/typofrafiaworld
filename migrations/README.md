# 🚀 Миграция: Добавление каталога услуг типографии

## 📦 Что включено

Эта миграция добавляет **полный каталог услуг** из прайс-листа в базу данных:

### Категории услуг (12 категорий, 43+ услуги):

| Категория | Услуг | Описание |
|-----------|-------|----------|
| 📄 Печать документов | 4 | Ч/Б и цветная печать A4, A3 |
| 📋 Копирование | 3 | Ксерокопии, копирование паспорта |
| 📷 Сканирование | 2 | Сканирование, распознавание текста OCR |
| 💼 Визитки | 3 | Односторонние, двусторонние, с ламинацией |
| 📄 Листовки | 3 | Форматы A6, A5, A4 |
| 🖼 Плакаты | 4 | Форматы A3, A2, A1, A0 |
| 🏷 Наклейки | 4 | Бумажные, виниловые, стикерпаки |
| 🪧 Баннеры | 5 | Баннеры, Roll-up стенды |
| 📐 Чертежи | 5 | Печать чертежей разных форматов |
| 📸 Фотопечать | 4 | Печать фото, фото на документы |
| 📅 Календари | 3 | Квартальные, настенные, карманные |
| 🏆 Дипломы | 3 | Дипломы, сертификаты |

### Параметры услуг:
- ✅ **Размеры** (A6, A5, A4, A3, A2, A1, A0, custom)
- ✅ **Тиражи** (от 1 до 2000+ шт с прогрессивными скидками)
- ✅ **Плотность бумаги** (130, 150, 200, 250, 300, 350 г/м²)
- ✅ **Стороны печати** (односторонняя, двусторонняя)
- ✅ **Ламинация** (матовая, глянцевая)
- ✅ **Базовые цены** для всех услуг

## 🎯 Как запустить миграцию

### Способ 1: Веб-интерфейс (рекомендуется) 🌐

1. Откройте в браузере:
   ```
   http://your-domain.com/admin/run_migration.php
   ```

2. Введите пароль: `migration2026`

3. Нажмите кнопку "Запустить миграцию"

4. Дождитесь завершения (займет 10-30 секунд)

5. Проверьте результат - вы увидите:
   - ✅ Количество добавленных услуг по категориям
   - ✅ Статистику параметров
   - ⚠️ Предупреждения о дубликатах (это нормально)

### Способ 2: Командная строка (если есть доступ к SSH) 💻

```bash
cd /path/to/typofrafiaworld
php migrations/run_migrations.php
```

## 📊 Ожидаемый результат

После успешной миграции в БД появятся:

```
Услуги по категориям:
  Баннеры                        :   5 услуг
  Визитки                        :   3 услуги
  Дипломы                        :   3 услуги
  Календари                      :   3 услуги
  Копирование                    :   3 услуги
  Листовки                       :   3 услуги
  Наклейки                       :   4 услуги
  Печать документов              :   4 услуги
  Плакаты                        :   4 услуги
  Сканирование                   :   2 услуги
  Фотопечать                     :   4 услуги
  Чертежи                        :   5 услуг
------------------------------------------------------------
  ВСЕГО                          :  43+ услуг

Параметры:
  Размеры                        :  20+ записей
  Плотности                      :  18+ записей
  Стороны печати                 :  12+ записей
  Тиражи                         :  60+ записей
  Ламинация                      :   6+ записей
```

## 🌐 Проверка на сайте

После миграции:

1. **Главная страница** (`index.php`):
   - Откроется каталог с 12 категориями
   - Каждая категория - кликабельная карточка с иконкой

2. **Страница категории** (`catalog.php?category=Визитки`):
   - Список всех услуг категории
   - Поиск по услугам
   - Клик на услугу → переход на страницу с калькулятором

3. **Страница услуги** (`service.php?id=business_cards_std_1side`):
   - Калькулятор с выбором параметров
   - Автоматический расчет цены
   - Кнопка "Оформить заказ"

## 🔧 Изменения в БД

Миграция внесет следующие изменения:

### 1. Изменения схемы таблицы `services`:
```sql
ALTER TABLE services ADD COLUMN category VARCHAR(100);
ALTER TABLE services ADD COLUMN description TEXT;
ALTER TABLE services ADD COLUMN is_active TINYINT(1) DEFAULT 1;
ALTER TABLE services ADD COLUMN sort_order INT DEFAULT 0;
```

### 2. Изменения в `service_quantities`:
```sql
ALTER TABLE service_quantities ADD COLUMN quantity INT DEFAULT 1;
ALTER TABLE service_quantities ADD COLUMN price DECIMAL(10,2) DEFAULT 0;
```

### 3. Новая таблица `service_sides`:
```sql
CREATE TABLE service_sides (
  id VARCHAR(50),
  service_id VARCHAR(50),
  label VARCHAR(255),
  multiplier DECIMAL(5,2) DEFAULT 1.00,
  is_active TINYINT(1) DEFAULT 1
);
```

## ⚠️ Важные замечания

### Безопасность:
- ⚠️ **ОБЯЗАТЕЛЬНО** смените пароль в `admin/run_migration.php`
- ⚠️ После запуска миграции удалите или переименуйте файл `admin/run_migration.php`
- ⚠️ Не оставляйте файл миграции доступным в production

### Повторный запуск:
- ✅ Миграцию можно запускать несколько раз
- ✅ Дубликаты будут автоматически пропущены
- ✅ Новые услуги будут добавлены, существующие - обновлены

### Откат:
Если что-то пошло не так, можно восстановить из бэкапа:
```sql
-- Создайте бэкап ПЕРЕД миграцией:
mysqldump -u username -p database_name > backup_before_migration.sql

-- Восстановление:
mysql -u username -p database_name < backup_before_migration.sql
```

## 📝 Логи

Все ошибки логируются в:
- Браузер (веб-интерфейс)
- `logs/system.log` (если включено логирование)

## 🆘 Устранение проблем

### Проблема: "Таблица service_sides не существует"
**Решение:** Миграция создаст её автоматически

### Проблема: "Duplicate entry"
**Решение:** Это нормально! Дубликаты пропускаются автоматически

### Проблема: "Ошибка подключения к БД"
**Решение:** Проверьте настройки в `config/config.php`:
- DB_HOST
- DB_NAME
- DB_USERNAME
- DB_PASSWORD

### Проблема: "Услуги не отображаются на сайте"
**Решение:**
1. Проверьте что миграция прошла успешно
2. Очистите кэш браузера (Ctrl+F5)
3. Проверьте что поле `is_active = 1` для услуг
4. Проверьте наличие категорий: `SELECT DISTINCT category FROM services`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в браузере при запуске миграции
2. Проверьте `logs/system.log`
3. Проверьте структуру БД: `SHOW TABLES LIKE 'service%'`

## ✅ Контрольный список

После миграции проверьте:

- [ ] Миграция завершилась успешно (видны статистики)
- [ ] На главной странице отображаются 12 категорий
- [ ] При клике на категорию открывается список услуг
- [ ] При клике на услугу открывается страница с калькулятором
- [ ] Калькулятор показывает параметры (размеры, тиражи и т.д.)
- [ ] Расчет цены работает при выборе параметров
- [ ] Удален файл `admin/run_migration.php` (для безопасности)

## 🎉 Готово!

После успешной миграции ваш сайт типографии готов к работе со полным каталогом услуг!

---

**Commit:** 73b8d43
**Branch:** claude/clarify-session-scope-268sp
**Дата:** 2026-01-09
